<!--miniprogram/pages/login/login.wxml-->
<i-message id="IMessage"></i-message>
<view class="form">
    <form bindsubmit="handleSubmit">
        <view class="form-item">
            <text class="form-item-label">账号</text>
            <input model:value="{{username}}"
                placeholder="请输入通通锁用户登录账号"
                name="username"
                bindinput="handleInputEmpty"></input>
        </view>
        <view class="form-item">
            <text class="form-item-label">密码</text>
            <input model:value="{{password}}"
                password="true"
                placeholder="请输入登录密码"
                name="password"
                bindinput="handleInputEmpty"></input>
        </view>
        <view class="form-item">
            <button hover-class="btn-hover" form-type="submit">登录</button>
        </view>
        <view class="form-item">
            该demo仅用于展示通通锁微信小程序插件使用方法，使用时，基础库版本请设置在3.0.2及其以上
        </view>
        <view class="form-item">
            适用插件版本：3.1.0及其以上
        </view>
    </form>
</view>

