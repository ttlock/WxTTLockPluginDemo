<!-- 添加指纹 -->
<view class="block">
    <text class="title">添加指纹：{{keyInfo.lockAlias}}</text>
</view>
<form bindsubmit="handleSubmit">
    <view class="form-item">
        <text class="form-item-label">指纹名称：</text>
        <input model:value="{{name}}"
            placeholder="请输入指纹名称"
            name="name"
            bindinput="handleInputEmpty"></input>
    </view>
    <view class="form-item">
        <text class="form-item-label">永久开关：</text>
        <switch model:checked="{{permanent}}" type="switch" color="#2886f7"></switch>
    </view>
    <DatespanPicker wx:if="{{!permanent}}" model:value="{{dateSpan}}" id="dateSpan"
        label="有效期"
        start-placeholder="请选择开始时间"
        end-placeholder="请选择结束时间"
        bindinput="handleUpdateDateSpan"></DatespanPicker>
    <view class="form-item">
        <button form-type="submit" data-type="INIT">开始添加</button>
    </view>
</form>
<view class="description mv-24 mh-16">{{state}}</view>